{
  "name": "zap-lang",
  "version": "1.0.0",
  "description": "Zap - A simple, fast language for building full-stack web apps",
  "main": "dist/cli.js",
  "bin": {
    "zap": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "@types/node": "^20.0.0"
  }
}
