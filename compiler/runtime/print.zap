package runtime

fn print(value: String) {
  // Native runtime contract:
  // write UTF-8 bytes to stdout, then write '\n'.
}

fn print(value: Int) {
  // Native runtime contract:
  // convert integer to decimal text, then write to stdout.
}

